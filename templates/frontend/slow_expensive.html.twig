{% extends 'base.html.twig' %}

{% block body %}
    {# <script>
        window.user = {{ user|raw }};
    </script> #}

    <div id="homepage-app">

	{% for city in cities %}
    <p>{{city.name}}</p>
    <p>{{city.countrycode}}</p>
    <p>{{city.district}}</p>
    <p>{{city.country.name}}</p>

    {% endfor %}
    </div>
{% endblock %}
